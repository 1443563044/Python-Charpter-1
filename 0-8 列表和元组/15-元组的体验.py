'''
元组，元组和列表一样可以存储多个数据，但元组是小括号，且元组内的数据是不能修改的

列表可以修改：
num_list = [10, 20, 30]
num_list[0] = 100

一个元组可以存储多个数据，元组内的数据是不能修改的。
可以用来存储身份证等不能修改的数据。

'''

# 1.简单体验元组
t1 = (10, 20, 30)
print(t1)
print(type(t1))
